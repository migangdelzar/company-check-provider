import freeFixtures from "./fixtures/free.json" with {type:"json"}; import premiumFixtures from "./fixtures/premium.json" with {type:"json"}; import {validateConfiguration,type Configuration} from "./schema.js";
export function loadConfiguration(env:Record<string,string|undefined>=process.env):Configuration{const tier=env.PROVIDER_TIER;if(tier!=="free"&&tier!=="premium")throw new Error(`PROVIDER_TIER: must be one of: free, premium (received ${tier??"undefined"})`);return validateConfiguration({tier,fixtures:tier==="free"?freeFixtures:premiumFixtures,schedule:{rules:[],sequences:[{name:"default",actions:[{action:"respond",delayMs:0}]}]}})}
